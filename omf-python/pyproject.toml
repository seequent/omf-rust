[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "omf_python"
version = "0.1.0"
description = "A Python project for OMF"
requires-python = ">=3.7"

[tool.maturin]
features = ["pyo3/extension-module"]
